[project]
name = "mpose-mlops"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cesare Davide Pace", email = "104144011+CesareDavidePace@users.noreply.github.com" }
]
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.3.3",
    "scikit-learn>=1.7.2",
]

[project.scripts]
mpose-mlops = "mpose_mlops:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.5",
]
